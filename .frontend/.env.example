# ATENÇÃO: Este arquivo é apenas para referência
# As variáveis de ambiente devem ser configuradas no arquivo .env na RAIZ do projeto
# Este arquivo .frontend/.env.example é mantido apenas para documentação

# Veja o arquivo ../.env para configurar todas as variáveis de ambiente
# O frontend lerá as variáveis do .env raiz automaticamente
